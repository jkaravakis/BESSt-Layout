import { SiteHeader, SiteLogo } from '@tesla/design-system-react';

export const Header = () => {

  return (
      <SiteHeader>
        <SiteLogo appTitle='BESSt Layout' />
      </SiteHeader> 
  );
}